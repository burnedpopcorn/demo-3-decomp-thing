if (effect == 1)
{
    instance_create(effectx, effecty, obj_bangeffect)
    instance_create(effectx, effecty, obj_slapstar)
    instance_create(effectx, effecty, obj_baddiegibs)
}
if (effect == 2)
{
    instance_create(effectx, effecty, obj_bangeffect)
    instance_create(effectx, effecty, obj_slapstar)
}
